# 自然语言记账

个人自然语言记账系统：iPhone 快捷指令提交自然语言文本，后端解析为结构化交易并持久化，提供交易管理与收支统计。单用户，无用户系统。

## Language

### 交易

**交易 (Transaction)**:
一笔已入库的收支记录，包含金额、类型、一级分类、发生时间、备注和标签。
_Avoid_: 账单、流水、记录

**支出 (expense) / 收入 (income)**:
交易仅有的两种类型，没有第三种（如转账、退款）。

**一级分类 (Category)**:
交易归属的预设分类（如餐饮、交通、收入），统计按一级分类汇总。当前版本为固定预设集合，不支持用户自定义。
_Avoid_: 标签（标签是另一个概念）

**标签 (Tag)**:
挂在交易上的自由文本标记，一笔交易可有多枚，不参与统计汇总。
_Avoid_: 分类

### 录入方式

**自然语言记账 (Natural-language Entry)**:
手机快捷指令提交原始文本，由大模型解析后创建交易；使用快捷指令专用 Token。

**手工记账 (Manual Entry)**:
后台表单提交已经结构化的交易字段并直接写库，不调用大模型；手工交易的 `raw_text` 为空字符串，`confidence` 为空。
_Avoid_: 解析（手工记账没有解析步骤）

### 解析

**解析 (Parse)**:
把自然语言文本（如"午饭18块5"）转换为结构化交易的过程，由大模型完成。
_Avoid_: 识别、提取

**待确认 (requires_confirmation)**:
解析结果缺少金额时的状态。待确认结果不写入数据库，调用方需提示用户补充金额后重新提交。
_Avoid_: 失败、草稿

### 统计

**统计 (Summary)**:
按时间区间对已入库交易的纯计算聚合视图（收支总额、净额、分类汇总、每日支出），分今日统计与月度统计。统计永不调用大模型。
_Avoid_: 报表、分析

**默认时区 (Default Timezone)**:
判定"今日"与"月度"边界的时区。交易时间在库中以 UTC 保存，统计归日时按默认时区换算。
_Avoid_: 本地时间、服务器时区
